import Admin from './Admin';
import GeneralUser from './GeneralUser';
import Manager from './Manager';

/**
 * Role component wrappers only display if the user has appropriate
 * permissions to view the component.
 */

export default {
  Admin,
  GeneralUser,
  Manager,
};
